// const express = require('express');
// const router = express.Router();
// const dayController = require('../controllers/dayController');

// router.get('/user/:id', dayController.getAllDays, (req, res) => {
//   res.status(200).json(res.locals.days);
// });

// router.get('/:id', dayController.getDay, (req, res) => {
//   res.status(200).json(res.locals.day);
// });

// router.post('/', dayController.addDay, (req, res) => {
//   res.status(201).json(res.locals.day);
// });

// router.delete('/:id', dayController.deleteDay, (req, res) => {
//   res.status(200).json(res.locals.day);
// });

// // router.put('/:id', dayController.updateDay, (req, res) => {
// //   res.status(200).json(res.locals.day);
// // });

// module.exports = router;
